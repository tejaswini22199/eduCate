import {makeStyles} from '@material-ui/core/styles'
export default makeStyles((theme)=>({
    root: {
        '& .MuiTextField-root': {
          margin: theme.spacing(1),
        },
      },
    Form:{
        marginLeft:400,
        marginTop:20,
        marginBottom:10,
    },
   
}));
